# Twitter Launch Thread

## Thread (post from @expertvagabond)

**1/7**
Just launched $PAW on Solana.

Not another agent memecoin. This is the first token-gated MCP server on Solana.

Hold $PAW = access to our 9-agent AI swarm.

Here's how it works:

**2/7**
The OpenPaw swarm runs 9 AI agents:
- Claude Opus for quality-critical work
- Claude Sonnet for speed-sensitive tasks
- Claude Haiku for monitoring and indexing

6,800+ sessions. 132 memory chunks indexed in ChromaDB. Three-tier model fallback.

**3/7**
The MCP server checks your Solana wallet before every tool call.

getTokenAccountsByOwner → verify $PAW balance → grant or deny access.

No tokens, no tools. On-chain verification, not promises.

**4/7**
What you get with $PAW:

- Semantic search across our entire knowledge base
- Real-time agent status dashboard
- Share memory that all 9 agents can search
- Queue tasks with priority tiers (more $PAW = more agents)

**5/7**
Why this is different from SHELLRAISER ($5M to $19K) and CRUST (decorative token):

Those had zero utility. $PAW gates actual compute and knowledge access.

The MCP server is open source. Fork it for your own swarm.

**6/7**
Built with:
- Solana SPL token via pump.fun
- Python MCP server (stdio transport)
- Solana RPC for balance verification
- ChromaDB for semantic search
- OpenClaw agent framework

Works with Claude Code, Cursor, or any MCP client.

**7/7**
The code:
github.com/ExpertVagabond/paw-token

pump.fun:
[INSERT LINK]

This is what agent tokens should be — access keys to real infrastructure, not speculation vehicles.

$PAW
